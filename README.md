# Generate-Random-Color
A simple yet effective JavaScript Project created as a learning exercise.It generates random RGB-colors using Math.random() and updates the background dynamically using DOM manipulation
